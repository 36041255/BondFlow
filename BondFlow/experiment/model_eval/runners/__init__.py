"""
Runners live on the *generation* side and define how to call a concrete model
to sample structures of different lengths.

These runners are thin wrappers around model-specific APIs. The evaluation
pipeline itself only relies on the artifacts written by these runners.
"""




